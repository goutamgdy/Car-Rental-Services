name: Greeting

'on': [push, workflow_dispatch]

jobs:
  greet:
      runs-on: ubuntu-latest
          steps:
                - name: Say Hello
                        run: echo "Hello from GitHub Actions 👋"